package carisma.regulatory.ruleallocator.OLD;

import static org.junit.Assert.*;

import org.junit.Test;
//TODO (jk) REMOVE OLD STUFF
/*
import org.eclipse.swtbot.eclipse.finder.SWTWorkbenchBot;
import org.eclipse.swtbot.swt.finder.widgets.SWTBotButton;
import org.eclipse.swtbot.swt.finder.widgets.SWTBotLabel;
*/



public class AllocationViewTest {
	
	// TODO (jk) REMOVE OLD STUFF
	/*
	private final SWTWorkbenchBot bot = new SWTWorkbenchBot();
	
	
	 @Test
	 public void test() {
		    System.out.println(bot.viewById("de.sepenn.bachelor.app.AllocationView").toString());
		    SWTBotButton alloButton = bot.button("Allocate Chosen Items");
		    alloButton.click();
		    
		   SWTBotLabel ruleLabel = bot.label("Regel Element:");
		   assertEquals(ruleLabel.getText(), "Regel Element:");
	
		   SWTBotLabel ruleLabel2 = bot.label("Wähle Regel");
		   assertEquals(ruleLabel2.getText(), "Wähle Regel");
		  
		   SWTBotLabel BPMNLabel = bot.label("BPMN Element:");
		   assertEquals(BPMNLabel.getText(), "BPMN Element:");
		   
		   SWTBotLabel BPMNLabel2 = bot.label("Wähle BPMN Element");
		   assertEquals(BPMNLabel2.getText(), "Wähle BPMN Element");
		 
		   SWTBotButton refreshButton = bot.button("Refresh chosen BPMN Element");
		   refreshButton.click();
		   
		   SWTBotButton removeButton = bot.button("Remove Chosen Allocation");
		   removeButton.click();
		   
		 
		   
	
		} */

}
